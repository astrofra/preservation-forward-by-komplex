#ifndef FORWARD_OFFLINE_APP_MAKU_CAPTURE_H
#define FORWARD_OFFLINE_APP_MAKU_CAPTURE_H

#include "app/export_config.h"

namespace forward_offline {
int export_maku_capture(const ExportConfig& config);
}
#endif
